#!/usr/bin/env bash
set -euo pipefail

# ▣ [1] 공통 설정
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TMP_DIR="$SCRIPT_DIR/tmp/$(basename "$0" .sh)"
rm -rf "$TMP_DIR"
mkdir -p "$TMP_DIR"

source "$SCRIPT_DIR/utils.sh"

echo
echo "========================"
echo "$(basename "$0") Start"
echo "========================"
echo

cat <<'B'
=============================================
 레슨 20) 여기문서 (Here Document) & 템플릿 생성
=============================================
B

echo "[목표]"
echo "- cat <<EOF 문법을 이해하고,"
echo "- 변수 확장 / 비확장 차이를 눈으로 확인하며,"
echo "- 실제 설정파일 템플릿을 생성해본다."
echo

# ------------------------------------------------------------
echo "1️⃣  기본 형태: cat <<EOF"
NAME="Joshua"
cat <<EOF
Hello, $NAME!
오늘은 Here Document를 배웁니다.
EOF
f_pause

# ------------------------------------------------------------
echo "2️⃣  작은따옴표 버전: cat <<'EOF' (리터럴 모드)"
cat <<'EOF'
이 안의 $NAME 은 확장되지 않습니다.
즉, 결과에 그대로 '$NAME' 이 표시됩니다.
EOF
f_pause

# ------------------------------------------------------------
echo "3️⃣  들여쓰기 허용 버전: cat <<-EOF"
# 탭(tab) 문자를 무시하고 들여쓰기 가능
cat <<-EOF
	이 블록은 탭으로 들여쓰기 해도 출력엔 영향 없습니다.
	(단, 공백은 허용되지 않음)
EOF
f_pause

# ------------------------------------------------------------
echo "4️⃣  여기문서를 이용해 파일 생성"
cat <<EOF > config.txt
# Sample Config
USER_NAME=$NAME
HOST=localhost
PORT=8080
EOF

echo "생성된 config.txt 내용:"
cat config.txt
f_pause

# ------------------------------------------------------------
echo "5️⃣  sed 로 템플릿 변환 (HOST 교체)"
sed -E 's/HOST=.*/HOST=server.local/' config.txt > config_new.txt
echo "변환된 config_new.txt 내용:"
cat config_new.txt
f_pause

# ------------------------------------------------------------
echo "6️⃣  awk 로 설정파일 요약"
awk -F= 'NF==2{printf "%-10s → %s\n",$1,$2}' config_new.txt
f_pause

# ------------------------------------------------------------
echo "7️⃣  grep 정규표현식으로 주석 제외 출력"
grep -vE '^\s*#' config_new.txt
f_pause

# 테스트용 데이터 삭제 여부 
f_delete_tmp

echo "레슨 20 완료 🎉"

echo "========================"
echo "$(basename "$0") End"
echo "========================"
